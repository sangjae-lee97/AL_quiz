import random

matrix_64x64_random = [
    "".join(str(random.randint(0, 1)) for _ in range(64))
    for _ in range(64)
]

def compress(r, c, size):
    compress(0,0,64)
color = data[r][c]

for color in matrix_64x64_random:


    


